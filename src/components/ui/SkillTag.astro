---
interface Props {
  skill: string;
}

const { skill } = Astro.props;
---

<span
  class="bg-primary/10 text-primary border-primary/20 hover:bg-primary/20 cursor-default rounded-full border px-4 py-1.5 text-sm font-bold shadow-sm transition-colors select-none"
>
  {skill}
</span>
