---
interface Props {
  skill: string;
}

const { skill } = Astro.props;
---

<span class="px-4 py-1.5 rounded-full bg-primary/10 text-primary font-bold text-sm border border-primary/20 shadow-sm hover:bg-primary/20 transition-colors cursor-default select-none">
  {skill}
</span>
